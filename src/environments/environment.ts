// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyBddh_C4JBd7EDaacYyPsmTb_dg-cz0jAA",
    authDomain: "fir-app-862f1.firebaseapp.com",
    databaseURL: "https://fir-app-862f1.firebaseio.com",
    projectId: "fir-app-862f1",
    storageBucket: "fir-app-862f1.appspot.com",
    messagingSenderId: "152477129999",
    appId: "1:152477129999:web:3e47424ba786569b"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
